Database:
-----------
>> Organized collection of related data which can be accessed and manipulated easily.

DBMS --> software used to work with DBs.

Relational Database Management System 
--------------------------------------
>> Data is stored in form of Rows and Cols (Tables)
>> SQL is must.
Ex: MySQL, Oracle, PostgreSQL...

DBMS 
------
>> Data is stored in form of document (BSON --> Binary JSON)
Ex: MongoDB, MariaDB...

MySQL 
------
>> Open Source Relational Database Management.

MySQL server --> stores DB, data...
MySQL Workbench --> IDE for MySQL
MySQL Shell --> Terminal for MySQL


Database --> Table ---> Data 
(Schema)

Entity --> Real time object
Attributes --> Information about entity.

actors --> name, age
actorDetails --> id, name, age

SQL --> Structured Query Language 

Syntax:
--------
>> Case-insensitive.(a=A)
>> Delimiter. statement terminator ";"

Datatypes 
-----------
Numeric Datatypes 
--------------------
INT --> whole numbers. 1,2,-1,-2..200
FLOAT --> Decimal Numbers. 1.22,3.14
BIGINT --> large integers 
DOUBLE --> large float.

String Datatypes 
-------------------
CHAR(n) --> fixed length character string. Ex: char(5) --> "hello", "hi hi"
VARCHAR(n) --> variable length character string. Ex: varchar(5) --> "q", "qw","qwe", "qwert"
TEXT --> paragraph

Constraints and Keys 
----------------------
Primary Key ---> No duplicates, No nulls. used to identify each records uniquely. (mysql will index the PK).
Foreign Key ---> It Relates 2 tables. refferential integrity.
FOREIGN KEY (col name) REFRERENCES col


NOT NULL --> no null values in it.
UNIQUE --> no duplicates.
AUTO_INCREAMENT --> generate sequential numbers.
CHECK --> add records conditionally Ex: CHECK (cond)
ENUM ---> enum (str1,str2)
DEFAULT

ACTIONS 
---------

Boolean 
-------
BOOLEAN --> 0 or 1

DATE and TIME 
---------------
DATE --> "yyyy-mm-dd"... "2026-08-07"
TIME --> "HH:MM:SS"
DATETIME --> Both

Operators
-------------
Comparison Operators
------------------------
= equal               
<> != not equal 
< lesser than 
> greater than 
<= lesser than or equal
>= greater than or equal

Logical Operators 
-------------------
AND
OR 
NOT 

IN, NOT IN 
--------------
Ex: ....col IN (v1,v2....)

BETWEEN (inclusive range)
---------
>> Range Filtering
Ex:..... col BETWEEN num1 AND num2;

LIKE 
-----
>> pattern matching.

Ex: .... col LIKE pattern;

patterns or wildcard 
-----------------------
% --> matches 0 to inf number of char.
_ --> matches only 1.

starts with v --> "v%" --> "Vinutha", "vijay"
ends with a --> "%a" --> "anitha", "a", "vijay"
contains a --> "%a%" --> "vijay"

"____a" --> "arya", "surya"


ORDER BY 
---------
Arranges the records 
Ex:........ORDER BY col ASC/DESC;

LIMIT/OFFSET 
---------------
L --> limit the number of records.
O --> skips the initial records;

Ex: ..... LIMIT num OFFSET num;

1000 results
--------------
1 --> 1-10
2 --> 11-20

Functions
------------
>> It is a pre defined operation in MySQL that takes input and returns a result.

Numeric Functions 
------------------
ROUND(num,dig) --> Rounds off a decimal number to nearest integer.
FLOOR(dec)
CEIL(dec)
ABS(num)
POWER(n1,n2)
SQRT(num)
MOD(n1,n2)

String Functions 
-------------------
CONCAT(str1,str2....)
length(str)
UPPER(str)
LOWER(str)
TRIM(str)
LTRIM(str)
RTRIM(str)
SUBSTRING(str, start, length)
REPLACE(str,subStr,newStr)

Date and Time 
---------------
curdate()
curtime()
now()
datediff(d1,d2)

Query 
------
To Create a Database ---> CREATE DATABASE dbName;
To Select a DB ---> USE dtabaseName;
To Create a Table ---> CREATE TABLE tbname (
    col1 dt constraints,
    col2 dt constraints,
    .
    .
);
To create records --> INSERT INTO tbname (col details) VALUES (),()....;
To Retrive data from a table --> SELECT (col details (*)) FROM tbname;
To Update a record --> UPDATE tbname SET col=val;
To Delete a record --> DELETE FROM tbname;

CRUD 
----------
Create --> INSERT
Read --> SELECT
Update --> UPDATE
Delete --> DELETE

SQL_SQFE_UPDATES --> update/delete Only one record. where clause is must. cond depends on PK.

WHERE 
-------
>> To do filteration.
Ex: SELECT,UPDATE,DELETE....... WHERE cond;
conditions are expressions that return boolean.
expression is combination of operators and operands.

cricketer 
------------
id(pk)
-------------------------------------------------------------------------------------------
1	Virat Kohli	        Batsman	    virat.kohli@example.com	        9876543210	150	1
2	Jasprit Bumrah	    Bowler	    jasprit.bumrah@example.com	    9876543211	120	1
3	Ravindra Jadeja	    All-Rounder	ravindra.jadeja@example.com	    9876543212	110	1
7	MS Dhoni	        Keeper	    ms.dhoni@example.com	        9876543213	100	1
8	Rohit Sharma	    Batsman	    rohit.sharma@example.com	    9876543214	130	1
9	Hardik Pandya	    All-Rounder	hardik.pandya@example.com	    9876543215	110	1
10	Bhuvneshwar Kumar	Bowler	    bhuvneshwar.kumar@example.com	9876543216	120	1
16	Sachin Tendulkar	Batsman	    sachin.tendulkar@example.com	9876543219	150	0

jersey 
----------
id(pk) jersey playerId(fk-->id(cricketer))
--------------------------
1       7       7
2       18      1
3       45      8
